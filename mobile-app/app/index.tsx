import { ThemedView } from "../components/ThemedView";

// This is the entry point of the app. It will be immediately replaced by the logic in _layout.tsx.
export default function Index() {
  return <ThemedView />;
}